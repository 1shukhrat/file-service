spring:
  application:
    name: file-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/file_service_db
    username: postgres
    password: postgres
    jpa:
      hibernate:
        ddl-auto: validate

